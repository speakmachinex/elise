# Elise

A simple chatbot inspired by Eliza, written in C.

You can build and run the binary with the following command.

```bash
gcc elise.c -o build/elise && ./build/elise
```

The current version does not handle punctuation and upper case well.
